# requirements_prod.txt  —  INFERENCE / EVAL ONLY
# Does NOT include: torch GPU, accelerate training, wandb
# torch (CPU-only) is installed separately in Dockerfile.prod Stage 1
transformers>=4.40.0
datasets>=2.18.0
evaluate>=0.4.0
huggingface_hub>=0.22.0
scikit-learn>=1.4.0
pandas>=2.0.0
numpy>=1.26.0
matplotlib>=3.8.0
seaborn>=0.13.0
